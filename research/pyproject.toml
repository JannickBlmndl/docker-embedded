[project]
name = "emb-cont"
version = "0.1.0"
description = "Embedded Containers - Workloads"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
    "pytest>=9.0.3",
]
